# 验证记录

验证日期：2026-09-23（America/Los_Angeles）

- 18 个讲义 Markdown 文件全部生成。
- 79 个 TikZ/pgfplots 图全部从原始源代码编译并导出为自包含 SVG；SVG 中的字形转换为路径，不依赖读者电脑上的 TeX 字体。
- 静态完整性检查通过：79 个图片引用全部存在，SVG XML 全部有效，内部引用目标全部解析，无转换占位符、项目私有数学宏、GitHub 专用数学围栏或 PDF 排版命令残留。
- MyST CLI `v1.11.0` 完整构建通过：19 个页面（目录 + 18 讲）全部生成，数学渲染与内部引用没有错误或警告。
- 在 Obsidian `1.13.7` 中实际打开目录和第 14 讲，正文行内/陈列公式、表格与内部导航均被识别。检查同时发现 Obsidian 不排版链接文字内部的 MathJax，故目录链接中的少量数学符号改用 Unicode，并把“链接标签不得含 `$`”加入自动验证。
- 对第 1、14、18 讲的代表性 SVG 做了栅格化目视抽查，坐标轴、数学标记、中文节点、线型和图例均清晰。

可重复的本地检查：

```bash
python3 tools/validate.py
myst build --html
```

`_build/` 是可再生成的构建缓存，不纳入阅读版交付。

## L03增量同步（2026-09-25）

- L03 Markdown与28页PDF已同步，其他讲未改。
- Markdown静态检查通过；PDF双遍编译无溢出、缺字或未定义引用警告，全页目检完成。
- 新增Obsidian原生块引用，其目标已检查；PDF中转为内部链接。本次未进行Obsidian交互测试或完整MyST网站构建，以上2026-09-23构建结果不是对当前原生块引用的兼容性验证。
